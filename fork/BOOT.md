# BOOT: [Название проекта]

*Читай этот файл первым в каждой сессии. Потом WAL.md.*
*Это конфигурация проекта — меняется редко, только при архитектурных решениях.*

---

## Проект

[2-3 предложения: что делает, кто использует, зачем существует]

**Стек:** [язык · фреймворки · ключевые библиотеки]
**Точка входа:** [например: main.py, src/main.rs, index.ts]
**Критические ограничения:** [например: нет OpenSSL, WASM-таргет, Python 3.10+]

---

## Порядок чтения (начало каждой сессии)

1. Этот файл (BOOT.md) — полностью
2. WAL.md — полностью
3. Спека из WAL#current-task — только указанную секцию
4. Код — только файлы из задачи, не читай подряд

---

## Структура проекта

```
[проект]/
├── BOOT.md
├── WAL.md
├── specs/
│   └── [FEAT/PROP файлы]
├── [src или lib]/
│   └── [основной код]
└── tests/
```

---

## Принятые архитектурные решения

*Не обсуждаются, не меняются без явного решения человека.*
*Если кажется что решение ошибочно → REVIEW-маркер, не трогать.*

| Решение | Причина |
|---------|---------|
| [Что выбрали и как устроено] | [Почему именно так] |
| [Что выбрали и как устроено] | [Почему именно так] |

---

## Протокол конфликтов

**Приоритет:** Человек > Спека > Код > Тесты

- Код не соответствует спеке → баг в коде
- Тест не соответствует спеке → баг в тесте
- Спека кажется ошибочной → `# REVIEW:` в коде, человеку на решение

---

## ЗАПРЕЩЕНО без явного запроса

- [ ] Изменять спеки (specs/)
- [ ] Трогать файлы вне текущей задачи
- [ ] Добавлять зависимости / библиотеки
- [ ] Удалять тесты
- [ ] «Улучшать» код за пределами задачи
- [ ] [Специфичные для проекта запреты]

---

## Стиль кода

- [Главный принцип: KISS / явное лучше неявного / и т.д.]
- [Соглашения по именованию]
- [Типизация: обязательна / желательна / нет]
- REVIEW-маркер: `# REVIEW: [описание] — причина: [почему]`

---

## Конфигурация AI-клиентов

*Этот файл работает с любым клиентом без изменений.*

- **Cline / Roo Code / Qwen Code:** AGENTS.md в корне → читается автоматически
- **Cline CLI:** добавь в `.clinerules` ссылку на BOOT.md и WAL.md
- **Claude (чат):** прикладывай BOOT.md + WAL.md вручную в начале сессии
